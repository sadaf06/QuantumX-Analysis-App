import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.quantum.cryptointelligence',
  appName: 'Quantum Crypto',
  webDir: 'dist'
};

export default config;
